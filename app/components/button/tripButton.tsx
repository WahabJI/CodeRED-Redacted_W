//this component is for the header at the top of the screen
const tripButton = (props: any) => {    
    return (
        <button className = "border-deep_marine hover:bg-deep_marine text-white border-2 text-center text-sm font-bold rounded-full w-6 h-6 ml-1 mt-6">
        </button>
    );
};

export default tripButton; 